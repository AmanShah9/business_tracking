import React from 'react';
import { Container, Typography } from '@mui/material';

const Reports = () => (
  <Container sx={{ mt: 3 }}>
    <Typography variant="h5">Reports</Typography>
    <Typography sx={{ mt: 2 }}>Reports and export features go here.</Typography>
  </Container>
);

export default Reports;
