import React from 'react';
import { Container, Typography } from '@mui/material';

const Payments = () => (
  <Container sx={{ mt: 3 }}>
    <Typography variant="h5">Payments</Typography>
    <Typography sx={{ mt: 2 }}>Payment functionality placeholder (list invoices, record payments).</Typography>
  </Container>
);

export default Payments;
